This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://github.com/vercel/next.js/tree/canary/packages/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `app/page.js`. The page auto-updates as you edit the file.

This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font), a new font family for Vercel.

## Learn More

To learn more about Next.js, take a look at the following resources:

- [Next.js Documentation](https://nextjs.org/docs) - learn about Next.js features and API.
- [Learn Next.js](https://nextjs.org/learn) - an interactive Next.js tutorial.

You can check out [the Next.js GitHub repository](https://github.com/vercel/next.js) - your feedback and contributions are welcome!

## Deploy on Vercel

The easiest way to deploy your Next.js app is to use the [Vercel Platform](https://vercel.com/new?utm_medium=default-template&filter=next.js&utm_source=create-next-app&utm_campaign=create-next-app-readme) from the creators of Next.js.

Check out our [Next.js deployment documentation](https://nextjs.org/docs/app/building-your-application/deploying) for more details.

</hr>

## run locally
open your code editor such as vs code 
now in left sidebar you will see a file logo click on it & it will show a option open folder click on it & select that folder where you want to pull this project, 
now in top bar left side you will see a view button click on it and it will open a sub menu where you can see a terminal sub menu click on it and it will open a terminal now run these commands to pull git hub repository but before pull you have to login on vs code using github account and you have to be authorised beacuse that is a private repository now as you have signed in run these commands in your terminal 
```bash
git clone 'url'
cd sbsgroups-web/my-app
npm install
npm run dev
```
if npm commands shows an eror such as npm.ps1

please use this command to install and run dev
```bash
npm.cmd install
npm.cmd run dev
```
now it will take some time to run the server, wheb server is running carefully you can see two links in your terminal first one is local host and second one is network where fromfirst one if you press `ctrl` button and press left mouse button it will open this website in your browser as localhost and you can see your webapplication locally on your system only if you wants to see your webapplication on your other devices then you have to do one thing that is that other devices should connected with some network on your system is connected now go to terminal and copy that another link of network and now you can run this website on other devices connected with same network...

